"use client";

import React from "react";

export default function AdminDashboard() {
  return (
    <div className="p-10">
      <h1 className="text-2xl font-bold italic">Checking Dashboard... (Phase 0 Fix)</h1>
      <p className="mt-4 text-gray-500 italic">If you see this, the 500 error is resolved and was caused by a specific UI component or icon.</p>
    </div>
  );
}
